Edit the .env by replacing your Database Connection and the JWT token also the SALT token.

cd server   ---- open different cmd terminal



cd client   ---- open different cmd terminal 

to run the app 


  npm start
